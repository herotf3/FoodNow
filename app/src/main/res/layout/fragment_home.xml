<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/color_background_darker">

        <android.support.design.widget.TabLayout
            android:id="@+id/home_tab_layout"
            android:layout_width="match_parent"
            android:layout_height="28dp"
            app:tabBackground="@drawable/tab_header_color_selector"
            app:tabSelectedTextColor="@color/tab_text_header_selected"
            app:tabTextColor="@color/common_google_signin_btn_text_light_default"
            app:tabIndicatorColor="@color/transparent"
            android:animateLayoutChanges="true"
            app:tabTextAppearance="@android:style/TextAppearance.DeviceDefault.Small"
            android:elevation="5dp"
            app:tabGravity="center"
            android:layout_gravity="center_vertical"
            android:backgroundTintMode="add"></android.support.design.widget.TabLayout>
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>